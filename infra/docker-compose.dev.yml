services:
  web:
    build:
      context: ../apps/web
      target: dev
    volumes:
      - ../apps/web:/app
      - /app/node_modules
      - /app/.next
    command: sh -lc "npm run dev -- -p 3000"
